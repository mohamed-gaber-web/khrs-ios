import { TopScoresPipe } from './top-scores.pipe';

describe('TopScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new TopScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
